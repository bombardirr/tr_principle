/**
 * Runtime feature flags — flip to false to hide UI without deleting code.
 *
 * Concordance: substring search over TM (no auto-apply on segment match).
 */
export const FEATURE_TM_CONCORDANCE = true
